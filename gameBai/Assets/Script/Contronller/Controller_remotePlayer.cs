using UnityEngine;

public class Controller_remotePlayer : MonoBehaviour
{
    public int numberOnHand;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
